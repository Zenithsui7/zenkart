kind = "web"
previewPath = "/"
title = "ZenKart"
version = "1.0.0"
id = "artifacts/zenkart"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19085

[services.development]
run = "pnpm --filter @workspace/zenkart run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/zenkart", "run", "build" ]
publicDir = "artifacts/zenkart/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19085"
BASE_PATH = "/"
